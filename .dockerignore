node_modules
.git
.env
.DS_Store
dist
coverage
npm-debug.log
yarn-error.log
pnpm-debug.log

# Ignore apps/frontend mostly since we only care about backend for this Dockerfile?
# No, context includes root, so it copies everything unless ignored.
# We explicitly COPY apps/backend in Dockerfile, but ignoring root node_modules is simpler.
apps/frontend/node_modules
apps/backend/node_modules
